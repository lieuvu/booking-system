{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Booking-Post",
  "description": "Booking schema of a booking system when making a post request.",
  "allOf": [
    { "$ref": "booking-common.json#/definitions/booking" },
    {
      "type": "object",
      "required": ["user_id", "machine_id", "start_time", "end_time"]
    }
  ]
}
