{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Machine-Put",
  "description": "Machine schema of a booking system when making a put request.",
  "allOf": [
    { "$ref": "machine-common.json#/definitions/machine" },
    {
      "type": "object",
      "minProperties": 1,
      "anyOf": [
        { "required": ["machine_type_id"] },
        { "required": ["brand"] },
        { "required": ["model"] },
        { "required": ["description"] }
      ]
    }
  ]
}
