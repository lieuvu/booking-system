{
  "name": "booking-system",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "private": true,
  "repository": "github:lieuvu/booking-system",
  "dependencies": {
    "slonik": "22.4.10",
    "slonik-sql-tag-raw": "1.0.2",
    "winston": "3.2.1"
  },
  "devDependencies": {
    "typescript": "3.9.3",
    "jest": "26.0.1",
    "@types/jest": "25.2.3",
    "ts-jest": "26.0.0",
    "ts-node": "8.10.2",
    "@types/slonik": "22.1.2",
    "@slonik/migrator": "0.5.0"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "compose:test": "docker-compose up --abort-on-container-exit",
    "compose:build": "docker-compose build --force-rm --no-cache --pull",
    "start:build": "tsc",
    "start": "node dist/test-slonik.js",
    "test": "jest --no-cache --runInBand --colors"
  }
}
